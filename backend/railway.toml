[build]
builder = "nixpacks"
buildCommand = "npx prisma generate && (npx tsc; true)"

[deploy]
startCommand = "npx prisma migrate deploy && node dist/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
